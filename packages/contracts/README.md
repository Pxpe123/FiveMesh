# FiveMesh contracts

This package stores schemas shared between the Engine, Server, and Web apps.

The current preview response is defined by `model-preview.schema.json`.

Future work should generate app-specific types from these schemas so the C# and
TypeScript contracts cannot drift apart.
